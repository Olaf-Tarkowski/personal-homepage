import { LoadingImage, LoadingSection } from "./styled";

const Loading = () => {
  return (
    <>
      <LoadingSection>
        <LoadingImage />
      </LoadingSection>
    </>
  );
};

export default Loading;
